mutation AddPostMutation {
  addPost {
    post {
      id
      title
    }
    query {
      first10Posts: posts(first: 10) {
        nodes {
          id
          title
        }
      }
    }
  }
}
